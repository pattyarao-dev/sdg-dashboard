import { DashboardAvailableIndicator } from "@/types/dashboard.types";

export default async function IndicatorDashboard({ goaldId, indicators }: { goaldId: string, indicators: DashboardAvailableIndicator[] }) {
  return (
    <div>
      hello
    </div>
  )
}
